export interface User {
  icon: string;
  firstName: string;
  lastName: string;
  xp: number;
  token: string;
  refreshToken: string;
  tokenExpiration: string;
  id: string;
  badgesCount: number;
}
