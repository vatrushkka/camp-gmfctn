import { PersonalAchievementsDatePipe } from './personal-achievements-date.pipe';

describe('PersonalAchievementsDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalAchievementsDatePipe();
    expect(pipe).toBeTruthy();
  });
});
